# Jarvis — Python Test Stability Repair — 2026-04-05T16:22:21Z

## Session Summary

Fixed Python smoke test failures and repaired skipped/not-run regression tests by adding dependency-tolerant import handling and correcting test bootstrap paths.

## Outcomes

1. **DatabaseService Import Fix**: Added try/except fallback for optional Neo4j driver imports to handle environments where neo4j package is not yet installed. Prevents import-time failures in smoke gate.

2. **Test Entrypoint Path Correction**: Repaired `.venv` bootstrap path resolution in test scripts (`test_database_schema.py`, `test_all_builds.py`) to resolve correctly from repo root.

3. **Python Validation Guidance Updated**: Added notes to `.github/instructions/python.instructions.md` on async test patterns, error handling in FastAPI, and import-time robustness.

4. **Validation Result**: All targeted smoke and regression tests passing after changes.

## Key Decisions Made

- Treat optional imports (Neo4j, others) as dependency-tolerant to improve test resilience.
- Ensure test bootstrap properly resolves repo root and venv paths relative to execution context.
- Document async/await patterns and error handling in Python instruction file for future maintenance.

## Impact

- Python test discovery now includes regression tests via pyproj.
- Smoke gate can run without full environment pre-setup.
- Future Python work has clearer guidance on robustness patterns.

## Files Changed

- `src/AspireApp.PythonServices/app/services/database_service.py` — added dependency-tolerant imports
- `test_database_schema.py`, `test_all_builds.py` — fixed path resolution
- `.github/instructions/python.instructions.md` — added validation guidance

