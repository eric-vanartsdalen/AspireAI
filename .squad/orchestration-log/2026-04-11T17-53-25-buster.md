# Orchestration Log: Buster (QA / Tester)

**Session:** 2026-04-11T17:53:25Z  
**Role:** Test framework debugging; separate app failures from orchestration issues  
**Mode:** Background (Non-blocking)  

## Spawn Context

- **Reason:** Reproduce failing auth/upload tests and determine whether fixture or app-level changes are needed
- **Outcome:** **ROOT CAUSE IDENTIFIED** — Shared Aspire fixture/database state (PostgreSQL checkpoint corruption, Neo4j store_lock collision) causes class-level crashes before test assertions; isolated non-fixture auth tests pass

## Work Completed

### Reproduction & Diagnosis
- Reproduced `AuthUxFoundationTests` and `AuthenticatedUploadUxTests` as class-level aborts before any individual test result reported
- Confirmed both failures traced back to `TestFixture` startup:
  - PostgreSQL repeatedly exited with `invalid checkpoint record` / `could not locate a valid checkpoint record`
  - Neo4j failed on later reruns with `/data/databases/store_lock` because repo-level bind mount was already in use
  - `AppHost` uses repo-level bind mounts (`database\postgres`, `database\neo4j\...`) instead of per-test isolation
- Verified auth logic seams remain healthy:
  - `CompositeAuthServiceTests` — **PASS** (service-layer auth composition)
  - `SignInPanelTests` — **PASS** (Blazor component auth UX)
  - `MockAuthServiceTests` — **PASS** (mock provider contract)
  - All three aligned with updated `MockAuthService` constructor (3-argument surface)

### Updated MockAuthService Test Constructor
- Reviewed `MockAuthServiceTests.cs` to align constructor calls with live 3-argument surface
- Verified constructor: `MockAuthService(tenantService, authContext, logger)`
- Tests now match implementation

### Required Follow-Up (Scribe Session)
- `TestFixture` must route PostgreSQL/Neo4j storage through isolated test paths OR
- AppHost must support cleanup/reset of shared repo-level stores before each WebTest run
- Until fixed: use non-fixture auth tests as signal for auth regressions

## Decision Log Entry

**Decision:** `.squad/decisions/inbox/buster-webtest-storage-hang.md`  
**Status:** Ready for Scribe merge

### Skill Updated
**File:** `.squad/skills/browser-smoke-fixture/SKILL.md`  
**Changes:** Documented fixture isolation issue and workaround (use service/component tests for auth signal while fixture state is shared)

## Key Outputs

- **Test Harness Issue Identified:** Shared Aspire storage causes class-level fixture crashes
- **App Logic Status:** ✅ Auth logic passes all non-fixture seams
- **Test Signal Recommendation:** Rely on `CompositeAuthServiceTests`, `SignInPanelTests`, `MockAuthServiceTests` until fixture isolation is resolved
- **Recommended Owner:** Bob or Jeff (to implement TestFixture isolation or AppHost cleanup path)

## Files Updated

- `src\AspireApp.WebTest\Tests\MockAuthServiceTests.cs` — Constructor alignment with live 3-arg surface
- `.squad/skills/browser-smoke-fixture/SKILL.md` — Fixture isolation workaround documented

---

**Signed:** Buster, QA / Tester  
**Status:** Work complete, ready for Scribe
