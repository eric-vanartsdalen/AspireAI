# Orchestration Log — Warden (Security Specialist) — 2026-04-05

**Date:** 2026-04-05  
**Agent:** Warden (Security Specialist)  
**Model:** claude-opus-4.6  
**Mode:** sync  
**Task:** Reviewed docs\AUTHENTICATION_SETUP.md against current auth code and corrected security/accuracy issues including dynamic Aspire ports, Google Console guidance, and honest current-state wording.

## Context

Warden was spawned immediately after Jeff to perform security review of the authentication documentation and code. The team's previous auth revisions had been rejected twice (Buster QA + route protection issues). This pass needed to verify no security vulnerabilities existed and documentation was accurate.

## Work Summary

1. **Security audit of Jeff's documentation:**
   - ❌ Found: Fabricated port numbers (7123/5123) that don't match Aspire config
   - ❌ Found: Deprecated Google+ API setup (Google shut down 2019)
   - ❌ Found: Unnecessary "Authorized JavaScript origins" for server-side OIDC
   - ✅ Verified: No secrets in committed configuration files
   - ✅ Verified: OIDC conditional registration prevents metadata failures

2. **Reviewed auth code for security posture:**
   - `AuthServiceFactory.ResolveServiceKey()` — Correctly falls back to mock when credentials missing
   - `MicrosoftEntraAuthenticationOptions.IsConfigured` — All three required secrets checked
   - Conditional OIDC registration — Only maps endpoints when secrets present
   - Mock endpoint gating — `/auth/mock/*` disabled when `Authentication:Service = "microsoft"`
   - Sign-out via `<a href>` (not `<button>`) — Works when Blazor circuit degraded

3. **Corrected documentation (`docs/AUTHENTICATION_SETUP.md`):**
   - Updated port guidance: Check Aspire dashboard for actual ports, register those in Azure Portal
   - Replaced Google+ API steps with current OAuth consent screen configuration
   - Removed "Authorized JavaScript origins" (not needed for server-side OIDC)
   - Fixed post-sign-in destination (redirects to `/`, not "dashboard")
   - Updated smoke test checklist (providers visible only in `auto`/`combined` modes)
   - Added Google testing-mode caveat for test-user-only restrictions

4. **Security assessment:**
   - ✅ OIDC handler conditional registration — Safe, prevents runtime errors
   - ✅ Factory resolution — Safe fallback to mock when unconfigured
   - ✅ Mock endpoint gating — Prevents auth bypass
   - ✅ No secrets in source — Correct storage in `dotnet user-secrets`
   - ✅ PKCE enabled — Defense-in-depth on auth code flow
   - ✅ Hardened cookies — 8-hour sliding expiration, SameAsRequest policy
   - ✅ Tenant trust boundary — External tenant claims not auto-trusted

## Decisions Produced

- `warden-auth-setup-guide-corrections.md` — Port, Google API, and redirect fixes
- `warden-auth-provider-factory.md` — Config-driven provider selection pattern
- `warden-microsoft-entra-security-defaults.md` — OIDC, cookie, and endpoint hardening
- `warden-mock-endpoint-gate.md` — Trust boundary between mock/live modes

## Impact on Future Work

- Documentation now provides accurate, dynamic port guidance
- Security defaults prevent common OIDC misconfiguration errors
- Mock endpoint gating prevents privilege escalation attacks
- Clear trust boundary between demo and live authentication modes

## Risk Assessment

| Risk | Mitigation | Status |
|------|-----------|--------|
| OIDC failures on wrong ports | Dynamic port guidance + dashboard check | ✅ Fixed |
| Deprecated Google setup | Updated to current OAuth flow | ✅ Fixed |
| Auth bypass via mock endpoints | Conditional registration gated by service mode | ✅ Fixed |
| Secret leakage | Enforcement: secrets in `dotnet user-secrets` only | ✅ Safe |

## Related Agents

- **Jeff** — Produced initial documentation; security review feedback incorporated
- **Buster** — QA regression tests remain valid; no code changes to auth logic
- **Bob** — Provider factory pattern supports security gating
