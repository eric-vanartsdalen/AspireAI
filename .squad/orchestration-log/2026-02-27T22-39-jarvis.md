# Spawn: Jarvis (Python/Data Dev) — 2026-02-27T22:39:00Z

## Mission
Fix save_document_page invocation/signature mismatch (P0 Item 1).

## Assignment
- **Agent:** Jarvis (Python/Data Domain Expert)
- **Task:** Correct `app/routers/processing.py` lines 67–75
  - Router passes `DocumentPage` object to `save_document_page()`
  - Method expects individual keyword arguments
  - Also fix FK value from `processed_doc_id` to `document_id`
- **Files Modified:** `src/AspireApp.PythonServices/app/routers/processing.py`
- **Files Read:** processing.py, database_service.py, models.py, decisions.md, jarvis/history.md

## Outcome
✅ **SUCCESS**

### Changes Applied
- **Line 75 (before):** `db.save_document_page(page_record)` 
- **Line 75 (after):** `db.save_document_page(file_id=document_id, page_number=page_num, content=page_content, metadata=page_metadata, neo4j_node_id=node_id)`
- **FK fix:** Corrected from `processed_doc_id` (return value of `save_processed_document()`) to `document_id` (original file ID from files table)

### Root Cause
The `save_document_page` method signature expects individual positional/keyword args:
```python
def save_document_page(self, file_id, page_number, content, metadata, neo4j_node_id):
```

But the router was constructing a `DocumentPage` Pydantic object and passing it as a single argument. The FK column `document_pages.file_id` is a foreign key to `files.id`, not to processed documents, so the correct value is the original `document_id` passed in the processing function.

### Commit
- **Ref:** `e9d90ea` on `feature/doc-upload`
- **Message:** "Fix save_document_page invocation: pass individual args instead of DocumentPage object; correct FK from processed_doc_id to document_id"

## Impact
- **Pipeline Effect:** Unblocks document page persistence during processing
- **Cross-Agent:** Updates Bob's orchestration log and Jeff's coordination items (file discovery now unblocked pending status casing fix)
- **Risk:** None; method signature unchanged, only caller corrected

## Files Involved
- **Modified:** `src/AspireApp.PythonServices/app/routers/processing.py` (1 edit, lines 67–75)
- **Read:** `app/services/database_service.py` (verified method signature), `app/models/models.py` (DocumentPage schema), `.squad/decisions.md` (context)

## Next Steps
- **Gate B1 (File Discovery):** Pending Jeff's status casing fix in FileUploadController
- **Gate B2 (Processing Pipeline):** Pending Bob's router contract rewrite (missing ~10 methods)
- **Validation:** Manual test of upload → processing → page storage once all P0 fixes land
