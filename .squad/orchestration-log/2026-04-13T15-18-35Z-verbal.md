# Orchestration Log: Verbal

**Agent:** Verbal (Product / Roadmap)  
**Spawn Time:** 2026-04-13T15:18:35Z  
**Task:** P1 Impact Assessment for Upcoming BRAIN Phase  
**Mode:** background

---

## Execution Summary

**Outcome:** ROADMAP IMPACT REVIEW — Recommended reframing three P1 items from "done" to "foundation-only" to reduce Phase 2 execution risk.

### Findings by Item

#### ✅ Items That Remain Valid as Complete

- **Export Docling free-text output to markdown and stage it for LightRAG document scanning**
  - Markdown staging works; BRAIN Phase 2 supersedes with `CanonicalDocument` contract
  - Risk Level: LOW
  - Verdict: KEEP AS DONE

- **Prove a live LightRAG ingest to query round-trip against the running container**
  - Handoff proven; LightRAG query returns results; Phase 2 introduces `LightRAGRetriever` as pluggable implementation
  - Risk Level: LOW
  - Verdict: KEEP AS DONE

- **Confirm AppHost LightRAG graph storage stays on the explicit Neo4j contract at runtime**
  - LightRAG wired in AppHost; schema coexistence documented in Plan.md risk #5
  - Risk Level: LOW
  - Verdict: KEEP AS DONE

#### ⚠️ Items Requiring Restatement: Foundation Only (Not Execution-Ready)

1. **Keep orchestration through Python retrieval APIs (no parallel retrieval path)**
   - Reality: Python `/documents/` endpoint exists; no retrieval layer yet
   - Risk: Marking "done" implies retrieval contract is finalized, but actual retrieval architecture undefined until Phase 2 (`POST /brain/query`)
   - Verdict: Reword to **"[Foundation] Proved Python processing contract; retrieval contract defined in Phase 2"**

2. **Process uploaded records through Docling and persist page content in `document_pages`**
   - Reality: Docling extraction works but no contract enforcement; BRAIN requires normalized `CanonicalDocument` with typed fields
   - Risk: Legacy processing persists raw text; BRAIN Phase 2 introduces strict contract with `source_confidence`, `tenant_id`
   - Verdict: Reword to **"[Foundation] Docling extraction proven; CanonicalDocument contract normalization deferred to Phase 2"**

3. **Persist processing timestamps and error details consistently**
   - Reality: Timestamps stored, but no structured error contract; BRAIN requires error state machine and `ReasonResponse` contract
   - Risk: Updating error persistence and status transitions is Phase 2 work, not complete in P1
   - Verdict: Reword to **"[Foundation] Timestamps recorded; error state machine and source attribution completed in Phase 2"**

### Roadmap Risks Identified

**High-Risk:** Retrieval Contract Ambiguity
- If Phase 0 assumes "retrieval contract is final" but Phase 2 discovers it must support confidence-aware ranking or multi-retriever dispatch, rework cascades
- Mitigation: Accept "retrieval via Python" as true but acknowledge contract shape is undefined; Phase 0 stubs endpoints; Phase 2 solidifies contract

**Medium-Risk:** Source Attribution Completeness
- Legacy data (pre-Phase 2) lacks `source_type` and `source_confidence`
- If Phase 2 makes these required, migration is needed
- Mitigation: Phase 0 decides to backfill with defaults or exclude pre-Phase-2 rows

**Low-Risk:** Container Orchestration
- LightRAG and BRAIN schema coexist in same Neo4j; potential for label collisions
- Mitigation: Documented as accepted risk; review in Phase 3+

### Recommendations

**Immediate (Before Phase 0 Kickoff):**
1. Reword three items in `roadmap/Tasks.md` P1 section (lines 58, 59, 68) to "Foundation Only" wording
2. Add to `roadmap/Plan.md` Risk Register: "Risk #6 — Legacy data migration"

**Phase 0 Tasks:**
- Gateway `/brain/query` endpoint as stub returning 501; document that contract will be finalized in Phase 2
- Preserve existing Python `/documents/` endpoint for backward compatibility

**Phase 2 Readiness:**
- Validate both `BrainKnowledgeRetriever` and `LightRAGRetriever` satisfy identical `IKnowledgeRetriever` interface
- Backfill SQLite `files` table with `source_confidence=0.7` and `source_type="upload"` defaults

### Decisions Recorded

**The P1 checklist is a strong foundation** — Docling parsing works, LightRAG integration is proven, container orchestration is stable. However, **three items assume execution-ready contracts that do not yet exist.** Rewording from "Done" to "Foundation Only" creates clearer accountability and prevents Phase 2 from discovering mid-sprint that P1's "done" checklist requires foundational rework.

---

## Deliverable

Decision inbox file written: `.squad/decisions/inbox/verbal-brain-phase.md`

