# FastAPI web framework and ASGI server
anyio==4.13.*
fastapi==0.135.*
uvicorn==0.42.*

# Document processing (CPU-only versions to avoid CUDA timeouts)
docling-core==2.70.*
pypdf2==3.0.*
python-docx==1.2.*
python-multipart==0.0.22
pydantic==2.12.*
aiofiles==25.1.*

# Database connections
neo4j==6.1.*
psycopg[binary]==3.3.*
psycopg-pool==3.3.*

# Logging and utilities
python-json-logger==4.0.*

# Test runner for the existing smoke and regression suite
pytest==9.0.*

# Agent framework for Phase 3b Critique mode (PydanticAI - swappable design)
pydantic-ai==0.0.*

# URL content extraction
httpx==0.28.*
beautifulsoup4==4.13.*
trafilatura==2.*

# YouTube support
youtube-transcript-api==0.8.*

# Note: docling and docling-ibm-models can cause long build times due to CUDA dependencies
# They will be installed in the full Dockerfile with proper timeout handling
